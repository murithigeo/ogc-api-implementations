"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/*

interface newFeature {
  geometry: {
    type: string;
    coordinates: [number, number, number];
  };
  id: string;
  properties?: {
    [key: string | number]: any;
  };
}

const newFeatures: newFeature[] = [];
for (let mountain of mountains) {
  newFeatures.push({
    geometry: {
      type: mountain.geometry.type,

      //@ts-ignore
      coordinates: [
        ...mountain.geometry.coordinates,
        mountain.properties.meters,
      ],
    },
    id: mountain.properties.name,
    properties: {
      regions: mountain.properties.regions,
      countries: mountain.properties.countries,
      continent: mountain.properties.continent,
      height_ft: mountain.properties.feet,
      height_m: mountain.properties.meters,
    },
  });
}

export { newFeatures };
*/ 
