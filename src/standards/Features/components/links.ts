import { Link } from "../../../types";



export async function generateLinks(mode: 'Collection'|'Landing'|'FeatureCollection'|'Feature'|'Conformance'|'Collections', context:ExegesisContext):Promise<Link>{
    let linkArray: Link[]=[]
    switch (mode){
        case 'Collection':
            
    }
    return 
}