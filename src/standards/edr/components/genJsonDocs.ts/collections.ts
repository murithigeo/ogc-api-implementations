import { ExegesisContext } from "exegesis-express";
import {Collection, CollectionWithoutProps} from "../../types";

async function genCollectionInfo(ctx: ExegesisContext,collectionConfig:CollectionWithoutProps): Promise<Collection> {
    
    if(collectionConfig.data_queries.area){
        Colle
    }
  return {
    title: collectionConfig.id + " collection",
    data_queries: {
        position:
    }
    //If extent[2] && extent[5] =0, then no vertical
  };
}
